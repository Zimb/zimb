# Place `zimb.png` here (extension icon, 128x128 PNG with transparency).
# Used by:
#   - package.json `chatParticipants[0].icon` (24x24 in chat)
#   - src/chat/participant.ts (vscode.Uri.joinPath(context.extensionUri, 'assets', 'zimb.png'))
